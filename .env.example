# Sanity project that holds properties and journal posts.
# Find the project id at https://sanity.io/manage
# These are public, read-only values. They are safe to commit to a deploy
# host's env settings, but keep write tokens out of this file.
VITE_SANITY_PROJECT_ID=
VITE_SANITY_DATASET=production
